# Frozen generation-zero references

These compact JSON files preserve the accepted three-process generation-zero
measurements from commit `03a14dcef1a0752e2a7cb2c214b38b4b4c152ecd`.
They contain only fields consumed by `scripts/latency-eval.sh score`; the raw
per-sample outputs remain local research evidence. Candidate write scopes must
exclude this directory, and the autoresearch identity locks every file here.

